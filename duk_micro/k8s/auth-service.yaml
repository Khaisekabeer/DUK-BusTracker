apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: bus-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: duk-bus/auth-service:latest
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: bus-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: bus-system
spec:
  selector:
    app: auth-service
  ports:
  - port: 80
    targetPort: 8000
